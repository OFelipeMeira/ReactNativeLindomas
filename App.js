// import Routers from "./routers";
import { Text, View } from "react-native";
import Routers from "./routers";

export default function App() {
  return (
    <Routers />

    // <View style={{justifyContent: 'center', alignItems:'center', flex:1}}>
    //   <Text style={{color: '#000', fontSize:30}}>teste</Text>
    // </View>

    // CRIAR BANCO DE USUARIOS
    // CRIAR BANCO DE IMAGENS

    //Criar 6 telas:
    /*
      -Login
      -Home
      -Create
      -Read
      -Update
      -Delete

      --todas devem ter uma barra de navegação, menos a Login
    */
  )
}